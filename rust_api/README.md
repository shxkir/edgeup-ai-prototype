# Rust API (Actix-Web Gateway)

This directory will contain the Rust gateway service built with Actix-Web. It will handle incoming HTTP requests, enforce API keys, log requests and responses, and forward calls to the Python AI service.
